{
  "name": "mor-score-backend",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --watch --env-file=.env server.js",
    "test": "vitest",
    "db:schema": "psql -d mor_score -f infrastructure/psql/schema.sql -U practice",
    "db:seed": "echo TODO: add this",
    "db:reset": "echo TODO: add this"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.14.1",
    "@2toad/profanity": "^3.2.0",
    "@stdlib/nlp-porter-stemmer": "^0.2.2",
    "collections": "^5.1.13"
  },
  "devDependencies": {
    "supertest": "^7.1.0",
    "vitest": "^3.1.2"
  }
}
